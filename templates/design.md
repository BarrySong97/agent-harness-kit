<!--
  design.md —— 设计系统(仅 UI 项目保留;纯后端/CLI 项目删除本文件及 AGENTS.md 中对应导航条目)。
  目的:让 agent 产出风格一致的界面。填写后删掉本注释。
-->

# <项目名> — 设计系统

## 设计 Token
- **色彩**:主色 `<#hex>`、辅助色 `<…>`、语义色(成功/警告/危险/信息)`<…>`、中性灰阶 `<…>`
- **字体**:字族 `<…>`、字号阶梯 `<12/14/16/20/24/32…>`、字重 `<…>`、行高 `<…>`
- **间距**:基准单位 `<4px / 8px>`,间距阶梯 `<…>`
- **圆角 / 阴影 / 边框**:`<…>`

## 布局
- 栅格 / 断点(响应式)`<…>`
- 容器最大宽度、页面留白 `<…>`

## 组件规范
> 列出项目通用组件的用法与状态,agent 据此复用而非重造。
- **按钮**:类型(主/次/文字/危险)、尺寸、状态(默认/hover/禁用/loading)`<…>`
- **表单**:输入、校验提示、错误态 `<…>`
- **反馈**:toast / 弹窗 / 空状态 / 加载态 `<…>`
- **<其他常用组件>**:`<…>`

## 交互与状态约定
- 加载、空、错误、成功 四态的统一处理方式 `<…>`
- 动效时长与缓动 `<…>`

## 无障碍(a11y)
- 对比度、焦点可见、键盘可达、语义标签 `<…>`
